[project]
name = "fortnite-discover-lab"
version = "0.1.0"
description = "Coleta e analise de colocacao no Discover do Fortnite"
requires-python = ">=3.9"
dependencies = [
    "curl_cffi>=0.7",
    "httpx>=0.27",
    "selectolax>=0.3",
    "pandas>=2.0",
    "pyarrow>=15",
    "duckdb>=1.0",
    "statsmodels>=0.14",
]
